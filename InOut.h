#include <string>
#include "Types.h"

#ifndef IN_OUT
#define IN_OUT

namespace inout {

  std::string askFileChoice(const char);

  int askMethodChoice(const index, const index);

  void generateSymmSystem(const index, const index);

}

#endif
